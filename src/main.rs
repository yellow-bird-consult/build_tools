// gets the files and folders in the current working directory
// let command = Command::new("ls").output().unwrap();
// println!("{}", String::from_utf8_lossy(&command.stdout));


fn  main() {
    println!("Hello deployment from Rust!");
}
